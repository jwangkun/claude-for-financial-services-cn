# China Pitch Agent — managed-agent cookbook

name: china-pitch-agent
model: claude-opus-4-7

system:
  file: ../../agent-plugins/china-pitch-agent/agents/china-pitch-agent.md
  append: "You are running headless. Produce files in ./out/; do not assume an open Office document."

tools:
  - type: agent_toolset_20260401
    default_config: { enabled: false }
    configs:
      - { name: read,  enabled: true }
      - { name: grep,  enabled: true }
      - { name: glob,  enabled: true }
  - { type: mcp_toolset, mcp_server_name: akshare, default_config: { enabled: true } }
  - { type: mcp_toolset, mcp_server_name: ifind,   default_config: { enabled: true } }
  - { type: mcp_toolset, mcp_server_name: wind,    default_config: { enabled: true } }

mcp_servers:
  - { type: url, name: akshare, url: "${AKSHARE_MCP_URL}" }
  - { type: url, name: ifind,   url: "${IFIND_MCP_URL}" }
  - { type: url, name: wind,    url: "${WIND_MCP_URL}" }

skills:
  - { from_plugin: ../../agent-plugins/china-pitch-agent }

callable_agents:
  - { manifest: ./subagents/researcher.yaml }
  - { manifest: ./subagents/modeler.yaml }
  - { manifest: ./subagents/deck-writer.yaml }
